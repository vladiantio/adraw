# vite-web-components

Minimal [Vite](https://vite.dev/) demo of [`@adraw/web-components`](../../packages/web-components) — the native `<adraw-canvas>` custom element with a plain-DOM toolbar driven through the element's `canvas` instance.

```bash
pnpm --filter=vite-web-components dev
```
